<!DOCTYPE html>
<html lang="en">
<head>
  <title></title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
      * {
          box-sizing: border-box;
      }

      body {
          font-family: Arial, Helvetica, sans-serif;
      }

      /* Style the header */
      header {
          background-color: #666;
          padding: 20px;
          text-align: center;
          font-size: 25px;
          color: white;
      }

      nav {
          float: left;
          width: 20%;
          height: 800px;
          background: #ccc;
          padding: 20px;
      }

      /* Style the list inside the menu */
      nav ul {
          list-style-type: none;
          padding: 0;
      }

      article {
          float: left;
          padding: 20px;
          width: 80%;
          background-color: #f1f1f1;
          height: 800px;
      }

      /* Clear floats after the columns */
      section::after {
          content: "";
          display: table;
          clear: both;
      }

      #authors {
          font-family: Arial, Helvetica, sans-serif;
          border-collapse: collapse;
          width: 100%;
      }

      #authors td, #authors th {
          border: 1px solid #ddd;
          padding: 8px;
      }

      #authors tr:nth-child(even){background-color: #f2f2f2;}

      #authors tr:hover {background-color: #ddd;}

      #authors th {
          padding-top: 12px;
          padding-bottom: 12px;
          text-align: left;
          background-color: #052290;
          color: white;
      }

      /* Responsive layout - makes the two columns/boxes stack on top of each other instead of next to each other, on small screens */
      @media (max-width: 600px) {
          nav, article {
              width: 100%;
              height: auto;
          }
      }
  </style>
</head>
<body>
<header>
  <h2>Edit Book</h2>
</header>

<section>
  <nav>
    <ul>
      <li><a href="<%= books_path %>">List All Books</a></li>
      <li><a href="<%= new_book_path %>">Add New Book</a></li>
    </ul>
  </nav>

  <article>
    <h1>New Author</h1>

    <%= form_for(:book, :url => {:action => 'update'}) do |form| %>
      <div>
        <%= form.label :book_title %><br>
        <%= form.text_field :book_title %>
      </div>
      <select class="selectpicker" multiple data-live-search="true">
        <% @authors.each do | author| %>
          <option value="<%= author.id %>"><%= author.first_name %></option>
        <% end %>
      </select>
      <div>
        <%= form.submit %>
      </div>
    <% end %>
  </article>
</section>
</body>
</html>